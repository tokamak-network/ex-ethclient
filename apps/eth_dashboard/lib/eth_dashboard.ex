defmodule EthDashboard do
  @moduledoc """
  Web-based real-time dashboard for ex_ethclient.

  Serves a browser-based live dashboard at http://localhost:4000 that
  auto-updates every second using Server-Sent Events (SSE), showing
  sync progress, peer connections, Engine API activity, recent blocks,
  network traffic, and BEAM system stats.
  """
end
